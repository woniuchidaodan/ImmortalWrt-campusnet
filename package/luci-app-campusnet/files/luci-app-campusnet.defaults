#!/bin/sh

[ -f /etc/config/campusnet ] && exit 0

cat > /etc/config/campusnet << 'EOF'
config config 'config'
	option enabled '0'
	option username ''
	option password ''
	option wifi_ssid ''
	option carrier 'auto'
	option interval '5'
	option timeout '10'
EOF

exit 0
